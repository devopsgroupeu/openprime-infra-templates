# @param decorators that sit above a line Injecto cannot rewrite.
# They parameterize nothing. Baselined so the gate fails only on NEW ones.
# Regenerate with: python tests/gate.py --write-baseline --injecto <path> --fixture <path>
#
# Emptied 2026-08-10 (OP-222). The 41 entries were all in
# templates/terraform/aws/_variables.tf, where @param sat above a
# `variable "x" {` block opener. Every one of those paths is also declared -
# and actually substitutable - in terraform.auto.tfvars, so the decorators
# were misleading duplication, not a functional gap. One contract, not two.
