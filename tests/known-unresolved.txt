# @param paths that no producer currently supplies, so the template default
# always ships. Baselined so the gate fails on NEW ones instead of staying red
# on known debt. This list should only ever shrink - every entry is a bug.
#
# Verified 2026-07-28 against openprime-app-backend prepareInjectoData
# (src/services/environmentService.js:294).
#
# Cleared 2026-08-10 (OP-222):
#   backend.s3.bucket  - re-pointed at terraformBackend.bucketName, the path the
#                        backend actually sends and the one templates/terraform
#                        _config.tf / _data.tf already use. `backend` in the
#                        Injecto payload is a BOOLEAN (terraform_backend.enabled),
#                        so backend.s3.* could never have resolved.
#   terraform.version  - de-decorated in both CI templates. Nothing sends a
#                        terraform.* namespace, and the templates pin
#                        required_version = "~> 1.11", so TF_VERSION: "1.11" is
#                        the intended value, not a placeholder. A decorator that
#                        lies is worse than an honest constant; version moves are
#                        OP-199's job.
#
# Cleared 2026-08-10 (OP-221):
#   services.eks.defaultNodeGroupIamAdditionalPolicies
#                      - de-decorated. Its value line opens a MULTI-LINE HCL map,
#                        and Injecto rewrites only the line following the
#                        decorator, so substituting it emitted `key = {"a":"b"}`
#                        and orphaned the map body plus its closing brace. Injecto
#                        now refuses that outright instead of corrupting the file,
#                        so the decorator became a promise it cannot keep. Re-add
#                        it only together with multi-line substitution support.
#
# Added 2026-08-20 (OP-227), and the first entries that are NOT bugs.
#
# The rule above -- "every entry is a bug" -- was written before the runtime
# catalog (OP-202). It assumed the frontend was the source of truth, so a @param
# no producer supplied could only be a decorator that lies. The entries below are
# the other case: the TEMPLATE is the source of truth and the frontend has not
# been regenerated from it yet. OP-208 hydrates the wizard from the catalog, at
# which point these become configurable without touching this file, and this
# block should be deleted rather than migrated.
#
# They surfaced only once the fixture stopped being authored from the templates
# (tests/derive_fixture.py). Before that the fixture supplied them and the gate
# could not see that no real producer does.
#
# Nothing is broken by them today: each is an addon version or ordering flag
# whose template default is the intended value. What was wrong was the silence.
#
# De-decorating them instead -- the terraform.version precedent above -- was the
# alternative, and was rejected because it would remove them from the catalog and
# so from the wizard OP-208 generates.
services.eks.addonCorednsMostRecent
services.eks.addonEbsCsiMostRecent
services.eks.addonEfsCsiMostRecent
services.eks.addonKubeProxyMostRecent
services.eks.addonPodIdentityBeforeCompute
services.eks.addonPodIdentityMostRecent
services.eks.addonVpcCniBeforeCompute
services.eks.addonVpcCniMostRecent
services.eks.defaultNodeGroupUseLatestAmi
